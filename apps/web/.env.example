# Next loads this if present. Prefer the repo-root .env (loaded via src/server/env.ts).
DATABASE_URL="file:../../prisma/dev.db"
SESSION_SECRET="dev-only-change-me-not-for-production"
NEXT_PUBLIC_DEFAULT_TENANT_SLUG="demo"
CNPJ_REGISTRY_ENABLED="false"
